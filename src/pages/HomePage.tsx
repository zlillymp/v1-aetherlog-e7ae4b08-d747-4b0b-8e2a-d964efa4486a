import { AetherLogApp } from '@/components/AetherLogApp';
export function HomePage() {
  // The main app component is self-contained.
  // This HomePage simply acts as the entry point.
  return <AetherLogApp />;
}